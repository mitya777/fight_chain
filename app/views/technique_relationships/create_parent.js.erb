$('.parent_techniques').prepend(
  "<%= escape_javascript(render :partial => 'techniques/parent_technique', :locals => {:parent => @parent, :child => @child}) %>");

$('.backdrop').remove();
$('.box').remove();
