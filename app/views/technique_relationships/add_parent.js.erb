if(!$('.backdrop').length){
  $('body').append('<div class = "backdrop"></div>');
  $('.backdrop').click(function(){
    if($('.box').length) $('.box').remove();
    $(this).remove();
  });
};

if(!$('.box').length)
  $('body').append('<div class = "box"><div class = "close">close</div>This is the lightbox</div>');

$('.box').html(
  '<h2>Chain Previous</h2>' +
  '<a class="fightbox_close" href="javascript:;">x</a>' +
  "<%= escape_javascript(render :partial => 'techniques/search_parents', :locals => {:child => @child}) %>" +
  "<%= escape_javascript( will_paginate @select_parents ) %> </br>" +
  "<div class = 'techniques_select'>" +
  "<%= escape_javascript(render :partial => 'parent_selection', :locals => {:child => @child, :select_parents => @select_parents}) %>" +
  "</div></br>" +
  "<%= escape_javascript( will_paginate @select_parents ) %>");

$('.fightbox_close').click(function(){
  $('.box').remove();
  $('.backdrop').remove();
});
