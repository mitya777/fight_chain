$('.child_techniques').prepend(
  "<%= escape_javascript(render :partial => 'techniques/child_technique', :locals => {:child => @child, :parent => @parent}) %>");

$('.backdrop').remove();
$('.box').remove();
