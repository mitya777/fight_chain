  <div class = "technique_candidate shadow">
    <%= render :partial => "videos/video", :locals => {:video => child.videos.first, 
                                                       :video_width => 150, :video_height => 122} %>
    <div class='thumb_name'>
      <%= link_to child.name, technique_relationships_path(
                :technique_relationship => {:child_id => child.id, :parent_id => parent.id}, 
                :origin => 'parent'), :title => "#{child.name}", :remote => true, :method => :post %>
    </div>
  </div>
